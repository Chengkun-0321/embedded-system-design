HW2/                 # 專案根目錄
├── web/                   # 後端 (Node.js + Express API)
│   ├── .DS_Store
│   ├── node_modules/      # 後端套件 (自動生成)
│   ├── app.js             # Express 伺服器入口 (包含 /api/username, /api/search 等路由)
│   ├── package.json       # 後端相依套件 & scripts
│   ├── package-lock.json  # 後端套件版本鎖定
HW2/
├─ package.json                 # 根目錄腳本與 concurrently 設定（一次啟動前後端）
├─ README.md                    # 專案說明（執行方式、API、技術與結構）
├─ 資料夾結構.txt               # 本檔，帶註解的資料夾樹
├─ 執行指令.txt                 #（可選）個人快速筆記
├─ 狀態碼.txt                   #（可選）狀態碼對照或備忘
├─ web/                         # 後端（Node.js + Express）
│  ├─ package.json              # 後端相依與 scripts（nodemon dev / node start）
│  ├─ app.js                    # 主要伺服器：中介層日誌、API 路由、啟動 4000 埠
│  ├─ data/
│  │  └─ 0_20000.csv           # 商品資料來源（CSV）
│  └─ services/                 # 後端服務模組
│     ├─ searchService.js       # CSV 搜尋（nodejieba 斷詞、權重與分頁）
│     └─ pdfPuppeteer.js        # 以 Puppeteer 將 HTML 轉 PDF（支援中文渲染）
│
├─ web-mgt/                     # 前端（React + Bootstrap 5）
│  ├─ package.json              # CRA 專案設定，含 proxy 指向 http://localhost:4000
│  ├─ public/
│  │  ├─ index.html            # CRA HTML 模板
│  │  ├─ manifest.json         # PWA/Meta 設定
│  │  └─ robots.txt            # 搜尋引擎爬蟲設定
│  └─ src/
│     ├─ index.js              # 入口檔，載入 Bootstrap CSS/JS 與 App
│     ├─ index.css             # 全域樣式（含 navbar/ footer 主題）
│     ├─ App.js                # 路由與頁面組合
│     ├─ components/
│     │  ├─ Navbar.js          # 原生 Bootstrap Navbar（藏藍主題、active 樣式）
│     │  └─ Footer.js          # 深藍灰底 + 線條風格 icon（GitHub/Email/LINE）
│     └─ pages/
│        ├─ Home.js            # 首頁（說明/導引）
│        ├─ page1.js           # 商品搜尋頁（呼叫 /api/search）
│        └─ page2.js           # 購物清單頁（加減數量、刪除、匯出 PDF）

備註：
- 前端以 `web-mgt/package.json` 之 `proxy` 直連後端 `http://localhost:4000`，開發時可直接呼叫 `/api/...`。
- 匯出 PDF 走 `/api/export-cart`，後端以 Puppeteer 產生 PDF，較能確保中文字型與表格樣式。
└── 資料夾結構.txt         # 專案檔案結構（此檔）


📌 說明
• web/
  • 負責 API (Express)
  • 包含路由範例：`/api/username`、`/api/search`
  • `npm run dev` → 自動重啟（nodemon）
  • `npm start` → 正式模式
• web-mgt/
  • 負責網頁 UI (React)
  • `npm start` → 跑開發伺服器 (http://localhost:3000)
  • `package.json` 中 `proxy` 指向 `http://localhost:4000`，以方便開發時跨域轉發
• root (HW2/)
  • 用 `concurrently` 同時啟動前後端
  • `npm run dev` → 開發模式：前端+後端一起跑

備註：
- `web` 是主要的後端程式所在資料夾，`data/0_20000.csv` 為搜尋資料來源。
- `web-mgt/src/pages/page1.js` 包含搜尋 UI，開發模式時 React StrictMode 可能會導致某些 useEffect 觸發兩次（僅開發時）。

快速啟動（在 Mac / zsh）
1) 安裝相依套件（在專案根目錄）
   npm install

2) 若要同時啟動前後端（concurrently）
   npm run dev

3) 單獨啟動後端
   cd web
   npm run dev   # 或 npm start（正式模式）

4) 單獨啟動前端
   cd web-mgt
   npm start     # 開發伺服器在 http://localhost:3000

常見錯誤與處理建議
- Proxy 錯誤 (ECONNREFUSED / ECONNRESET)
  • 症狀：前端 console 顯示 "Could not proxy request /api/... from localhost:3000 to http://localhost:4000"。
  • 原因：後端伺服器未啟動或在不同埠，導致 proxy 無法連線。
  • 處理：確認 `web` 在埠 4000 正常啟動；或檢查 `web-mgt/package.json` 的 `proxy` 是否正確。

- React 在開發模式呼叫 API 兩次
  • 症狀：後端 log 看到同樣的 GET 請求出現兩次（例如 `/api/username`）。
  • 原因：React 18 在開發環境下啟用 `StrictMode`，會做「掛載 → 卸載 → 再掛載」以檢測副作用，因此在 `useEffect(..., [])` 中的副作用會被執行兩次（僅開發）。
  • 建議：
    - 在開發短期可移除 `React.StrictMode`（`src/index.js` 或 `src/App.js`）以避免雙重呼叫，但 production build 不會有此行為。
    - 或在呼叫 API 的元件中使用 `useRef` 作一次性保護：
      const didFetch = useRef(false);
      useEffect(() => {
        if (didFetch.current) return;
        didFetch.current = true;
        // fetch...
      }, []);

- /api/search 找不到路由
  • 症狀：前端呼叫 `/api/search` 但後端回 404 或沒有回應。
  • 建議：在 `web/app.js` 中新增 `/api/search` 路由，並以 `web/services/searchService.js` 或直接讀 `data/0_20000.csv` 來回傳搜尋結果範例。

檔案與功能對照 (快速參考)
- `web/app.js`      : 啟動 Express Server，放 API 路由（/api/username, /api/search...）
- `web/services/searchService.js` : 搜尋邏輯（解析 CSV, 篩選關鍵字, 回傳分頁結果）
- `web/data/0_20000.csv` : 原始資料來源（商品或關鍵字清單）
- `web-mgt/src/pages/page1.js` : 搜尋畫面與分頁、加入購物車等 UI 行為
- `web-mgt/package.json` : dev server proxy 設定 (proxy -> http://localhost:4000)

若你希望我：
 - 直接在 `web/app.js` 新增一個簡單的 `/api/search` 範例實作（回傳 mock 分頁資料），我可以幫你做並執行快速測試；
 - 或把 `page1.js` 的 useEffect 加上 double-call 防護，我也可以修改該檔案。
